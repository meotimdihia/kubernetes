apiVersion: v1
kind: PersistentVolume
metadata:
  name: php-fpm-pv
spec:
  capacity:
    storage: 20Gi
  accessModes:
    - ReadOnlyMany
  persistentVolumeReclaimPolicy: Recycle
  gcePersistentDisk:
    # This disk must already exist.
    pdName: disks20
    fsType: ext4